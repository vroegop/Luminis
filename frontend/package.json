{
  "name": "frontend",
  "version": "0.0.1",
  "description": "Luminis Customer Statement Processor",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "polymer build --js-minify --css-minify --html-minify --bundle --js-compile",
    "serve": "polymer serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit-element": "^2.3.1"
  }
}
